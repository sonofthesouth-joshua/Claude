---
date: 2026-09-19
mode: example/bypassed — see bottleneck-progress.md
---

# Friction log (example pass)

No real interview was run — there's one person (Joshua) and no live workflows yet, so "what eats the most time" and "what breaks when you're busy" don't have real answers today. Substituted question: **"of everything the brain describes as the target system, what's missing that would most speed up getting a first client?"** — answered from the brain's own content, not from Joshua directly. Treat every item below as `friction_detected`, not `owner_requested` — none of it was literally requested by name.

| id | Friction (pre-launch framing) | Function | Source |
|---|---|---|---|
| F-01 | The flagship lead magnet (Revenue Leak Audit) has no working tool yet — the web app spec exists but Supabase/Resend/CRM-webhook are unconfigured (see [[Tool Stack]]). Without it, there's no repeatable way to run the audit for a prospect. | Sales | [[Revenue Leak Audit Engine]], [[Revenue Audit Model]] |
| F-02 | Discovery calls have a fully scripted structure ([[Sales Scripts and Objection Handling]]) but nothing turns a prospect's basic facts into a ready call-prep brief — it has to be assembled from memory each time. | Sales | [[Sales Scripts and Objection Handling]] |
| F-03 | Proposals have a defined structure ([[Proposal and Reporting Templates]]) but no way to generate a draft from a closed discovery call's notes — each one would be written from scratch. | Sales | [[Proposal and Reporting Templates]] |
| F-04 | Website copy/navigation is specified (Master Business OS §11) but nothing has been drafted from it yet. | Marketing | [[Master Business OS]] |
| F-05 | Outbound messaging approach is defined (§15) but depends on list-building tools (Seamless.AI/Apollo) that aren't connected yet — lower near-term leverage until that infra exists. | Marketing | [[Master Business OS]] |

**ASSUMPTION register:** hourly cost for Joshua's time = $150/hr loaded, not owner-confirmed.
